syntax = "proto3";

package identity;

option go_package = "github.com/authaas/types/identity";

// Principal is the identity a token's sub claim names.
message Principal {
  // A UUIDv4 in its canonical 36-character form. Its 16 raw bytes are the
  // WebAuthn user handle.
  string id = 1;
}
